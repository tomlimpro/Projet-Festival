@model FestivalAPI.Models.FestivalData

@{
    ViewData["Title"] = "Index";
}
<p><br /></p>
<p><br /></p>
<p><br /></p>
<h1>Créer une scène</h1>
<p>
    <a asp-action="Create">Créer une scène</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Nom de la scène</th>
            <th>Capacité</th>
            <th>Accessibilité</th>
            <th>Artistes</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Scenes)
        {
            string selectedRow = "";
            if (item.SceneID == (int?)ViewData["SceneID"])
            {
                selectedRow = "table-success";
            }
            <tr class="@selectedRow">

                <td>
                    @Html.DisplayFor(modelItem => item.Nom_Scene)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Capacite)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Accessibilite)
                </td>

                <td>
                    @foreach (var artiste in item.Artistes)
                    {
                        @artiste.Nom_Artiste <br />
                    }
                </td>

                <td>
                   
                    <a asp-action="Edit" asp-route-id="@item.FestivalID">Modifier cette scène</a> |
                    <a asp-action="Details" asp-route-id="@item.FestivalID">En savoir plus.</a> |
                    <a asp-action="Delete" asp-route-id="@item.FestivalID">Supprimer</a>
                </td>
            </tr>
        }
    </tbody>
</table>
